#pragma once
#include <string>
#include <sstream>
#include <fstream>

namespace Yumi 
{ 
    using String = std::string; 
    using StringStream = std::stringstream;
    using IStringStream = std::istringstream;
}